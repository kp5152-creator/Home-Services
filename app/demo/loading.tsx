import { RouteLoading } from "@/components/RouteLoading";

export default function DemoLoading() {
  return (
    <RouteLoading
      eyebrow="EstateIQ app"
      title="Preparing your workspace."
      description="EstateIQ is loading properties, inspections, reports, and workflow context."
    />
  );
}
